{
  "name": "helms",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ helms",
    "create-local": "graph create --node http://localhost:8020/ helms",
    "remove-local": "graph remove --node http://localhost:8020/ helms",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 helms",
    "test": "graph test"
  },
  "dependencies": {
    "@amxx/graphprotocol-utils": "^1.1.0",
    "@graphprotocol/graph-cli": "0.33.1",
    "@graphprotocol/graph-ts": "0.27.0",
    "as-base64": "^0.2.0",
    "as-bigint": "^0.5.3",
    "as-hex": "^1.0.1",
    "assemblyscript-json": "^1.1.0"
  },
  "devDependencies": {
    "matchstick-as": "0.5.0"
  }
}
